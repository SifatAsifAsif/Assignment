 #### 1) What is the difference between `null` and `undefined`?

Answer :  Null means no avalble value. undfined means not defined yet 

#### 2) What is the use of the `map()` function in JavaScript? How is it different from `forEach()`?

Answer :  map need when we do something on every elemnt and it returns value.

forEach is same type like when we need some opeation to do 
on every elemntn but we don't need retrun value

#### 3) What is the difference between `==` and `===`?

Answer :  == menas no type check only value cheak ( dangarous)

=== means type and value check at the same time 


#### 4) What is the significance of `async`/`await` in fetching API data?

Answer : 

async is asyncronise fucntion 

awite means wait for the response form the api 


#### 5) Explain the concept of Scope in JavaScript (Global, Function, Block).

gobal means acessable to all 

function or local means data is acessable inside the fucntion area or block 


block mean a specfic block like if block

